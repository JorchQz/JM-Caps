{
  "name": "jm-caps",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @jm-caps/admin",
    "dev:tienda": "npm run dev --workspace @jm-caps/tienda",
    "build": "npm run build --workspace @jm-caps/admin",
    "build:tienda": "npm run build --workspace @jm-caps/tienda",
    "typecheck": "npm run typecheck --workspace @jm-caps/admin && npm run typecheck --workspace @jm-caps/tienda",
    "gen:types": "npx supabase gen types typescript --project-id ndsnftmzmjsxxjdlwozk > packages/db/src/types.ts"
  },
  "engines": {
    "node": ">=20.19"
  },
  "devDependencies": {
    "wrangler": "^4.134.0"
  }
}
